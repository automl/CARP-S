# @package _global_
defaults:
  - base
optimizer_id: SyneTune-MOREA
optimizer:
  optimizer_name: 'MOREA'
  optimizer_kwargs:
    random_seed: ${seed}
  # max_budget: ${task.max_budget}



#      walltime_limit: 60,  # After 60 seconds, we stop the hyperparameter optimization
#      n_trials: 500,  # Evaluate max 500 different trials
#      n_workers: 8,