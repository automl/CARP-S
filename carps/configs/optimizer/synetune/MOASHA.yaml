# @package _global_
optimizer_id: SyneTune-MOASHA
optimizer_container_id: SyneTune
optimizer:
  _target_: carps.optimizers.synetune.SynetuneOptimizer
  _partial_: true

  optimizer_name: 'MOASHA'
  optimizer_kwargs:
    metrics: ${task.objectives}
    time_attr: ${problem.budget_type}
    #    random_seed: ${seed}
    #    max_resource_attr: ${problem.budget_type}
    # max_resource_level: ${task.max_budget}
    grace_period: ${task.min_budget}
    max_t: ${task.max_budget}
    reduction_factor: 3
    mode: 'min'
    brackets: 5
  #max_budget: ${task.max_budget}



#      walltime_limit: 60,  # After 60 seconds, we stop the hyperparameter optimization
#      n_trials: 500,  # Evaluate max 500 different trials
#      n_workers: 8,