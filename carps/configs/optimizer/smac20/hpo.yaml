# @package _global_
defaults:
  - base
optimizer_id: SMAC3-HPOFacade  # Hyperparameter Optimization (Bayesian Optimization) with Random Forest
optimizer:
  smac_cfg:
    smac_class: smac.facade.HyperparameterOptimizationFacade