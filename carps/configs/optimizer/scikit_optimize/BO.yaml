# @package _global_
defaults:
  - base
optimizer_id: Scikit_Optimize
optimizer:
  skopt_cfg:
    # See skopt.optimizer.optimizer.Optimizer for more details
    base_estimator: GP
    acq_func: gp_hedge
    acq_optimizer: auto


