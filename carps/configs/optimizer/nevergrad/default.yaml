# @package _global_
optimizer_name: "NGOpt"

optimizer_id: Nevergrad
optimizer_container_id: Nevergrad
optimizer:
  _target_: carps.optimizers.nevergrad.NevergradOptimizer
  _partial_: true
  nevergrad_cfg:
    optimizer_name: ${optimizer_name}
    seed: ${seed}
  optimizer_cfg: null
