# @package _global_
optimizer_id: Nevergrad-Hyperopt
optimizer_container_id: Nevergrad
optimizer:
  _target_: carps.optimizers.nevergrad.NevergradOptimizer
  _partial_: true
  nevergrad_cfg:
    optimizer_name: "Hyperopt"
    seed: ${seed}
  optimizer_cfg: null
