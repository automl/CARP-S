# @package _global_
optimizer_id: Nevergrad-NoisyBandit
optimizer_container_id: Nevergrad
optimizer:
  _target_: carps.optimizers.nevergrad.NevergradOptimizer
  _partial_: true
  nevergrad_cfg:
    optimizer_name: "NoisyBandit"
    seed: ${seed}
  optimizer_cfg: null
