# @package _global_
data_dir: "./data"
prior: null
perturb_prior: null

benchmark_id: MFPBench
problem_id: mfpbench/jahs/CIFAR10
problem:
  _target_: carps.benchmarks.mfpbench.MFPBenchProblem
  benchmark_name: "jahs"
  benchmark: "jahs"
  budget_type: "epoch"
  prior: ${prior}
  perturb_prior: ${perturb_prior}
  benchmark_kwargs:
    task_id: "CIFAR10"
    datadir: ${data_dir}
task:
  n_trials: 100
  n_objectives: 1
  min_budget: 3
  max_budget: 200
