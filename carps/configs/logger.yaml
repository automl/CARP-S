# @package _global_
loggers:
  - _target_: carps.loggers.file_logger.FileLogger
    _partial_: True
    overwrite: True
    # outdir: ${outdir}/${seed}
  - _target_: carps.loggers.database_logger.DatabaseLogger
    _partial_: True

